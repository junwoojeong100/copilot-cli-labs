# ===== 로컬 테스트용 환경변수 (RAG 에이전트) =====
# Foundry에 배포하면 런타임이 아래 값을 자동 주입합니다.
# 로컬에서 `azd ai agent run` 또는 직접 실행할 때만 이 파일이 필요합니다.
# 이 파일을 복사해 .env로 저장 후 실제 값을 입력하세요: cp .env.example .env
#
# 전제: 검색 인덱스가 이미 시드되어 있어야 합니다.
#       루트의 `src/06_rag_agent.py`를 한 번 실행하면 인덱스(maf-lab-knowledge-v1)가
#       자동 생성·시드됩니다.

# ── Foundry 호스팅 표준 환경변수 (배포 시 런타임이 자동 주입) ──
# 형식: https://<resource>.services.ai.azure.com/api/projects/<project>
FOUNDRY_PROJECT_ENDPOINT=https://your-resource.services.ai.azure.com/api/projects/your-project

# 배포된 채팅 모델 이름 (예: gpt-5.4, gpt-4.1)
AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-5.4

# ── 로컬 폴백 (FOUNDRY_* 변수가 없을 때 사용) ──
# PROJECT_ENDPOINT=https://your-resource.services.ai.azure.com/api/projects/your-project
# MODEL_DEPLOYMENT_NAME=gpt-5.4

# ── Azure AI Search (RAG 검색) ──
# Azure AI Search 서비스 엔드포인트
SEARCH_SERVICE_ENDPOINT=https://your-search-service.search.windows.net

# RAG 인덱스 이름 (기본값: maf-lab-knowledge-v1)
SEARCH_INDEX_NAME=maf-lab-knowledge-v1

# ── Azure OpenAI 임베딩 ──
# 임베딩 호출용 Azure OpenAI 엔드포인트
# 형식: https://<resource>.cognitiveservices.azure.com/
AZURE_OPENAI_ENDPOINT=https://your-resource.cognitiveservices.azure.com/

# 임베딩 모델 배포 이름 (예: text-embedding-3-large)
EMBEDDING_DEPLOYMENT_NAME=text-embedding-3-large

# Azure OpenAI API 버전
AZURE_OPENAI_API_VERSION=2024-10-21
